import ViewContactsList from "./ViewContactList";
import EditableContactList from "./EditableContactList";

export const ContactsList = {
  View: ViewContactsList,
  Selector: EditableContactList,
};

export default ContactsList;
